- x minimize the site
- x install sass
- x make app elemnent variable safe
	- x get element with class instead of id
- x install tailwind
- x button/event in Vite Vanilla
- x on button click, get value from input and insert in output
- x implement localstorage
	- x save value on button click
	- x read value on page load
- x build Router and pages
- x form code to form page
- x style menu and site
- .. create todo list page
	- x create page in menu etc.
	- x create html for page: input, button "Add"
	- x create mock event handler to alert "clicked" when clicked
	- .. attach events: when button add click, then add text in input
		- .. on click, add a test li "test li", each time clicked a new one
	- create delete button next to each todo item which deletes item
	- store in localstorage 


- .. build CLI to create a page
	- x when you type "npm run cp" it shows "creating page"
	
	 